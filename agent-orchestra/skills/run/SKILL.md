---
description: Orchestrate a task as an observable Agent Team. The orchestrator leads — forming a team of dynamic workers plus a standing reviewer and adversarial critic — and you watch them collaborate live in cmux panes. Use when coordinating real work in a project that already ran /agent-orchestra:init.
disable-model-invocation: true
---

# /agent-orchestra:run — orchestrator (team lead)

> **STATUS: skeleton stub.** Full orchestrator logic — reconcile loop, mandatory
> team formation, MEMORY.md injection, review/critic gate — is implemented in
> **Task #7** (and #8). See `DESIGN.md` §4–§5, §7, §9.

For now this stub only confirms the skill loads and is namespaced as `/agent-orchestra:run`.

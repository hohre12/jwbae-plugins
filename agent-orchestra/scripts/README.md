# scripts/

Non-standard helper directory (not auto-discovered). Hooks invoke these via
`${CLAUDE_PLUGIN_ROOT}/scripts/...`.

Filled in **Task #9**: review-gate / team-presence / dangerous-command guards.
See `DESIGN.md` §4.
